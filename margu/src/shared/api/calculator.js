/**
 * @typedef {import('./index').SubjectItem} SubjectItem
 * @typedef {import('./index').SubjectPayload} SubjectPayload
 * @typedef {import('./index').Speciality} Speciality
 * @typedef {import('./index').CalculatorResult} CalculatorResult
 */

export {
  fetchCalculatorResults,
  fetchCalculatorSubjects,
  fetchSubjects,
} from './index';
